import { createUseStyles } from "react-jss";

export const AppStyle = createUseStyles({
  app: {
    height: "50vh",
    width: "300px",
    margin: "0 auto",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
});
